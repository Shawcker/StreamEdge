%--- Erlang Inet Configuration -------------------------------------------------

% Add hosts by filling in their ip addresses on the network and hostnames
% Multiple hosts can be added
% {host, {_,_,_,_}, ["hostname1"]}.
% {host, {_,_,_,_}, ["hostname2"]}.

{host, {192,168,43,238}, ["my_grisp_board"]}.

% Do not monitor the hosts file
{hosts_file, ""}.

% Disable caching
{cache_size, 0}.

% Specify lookup method
{lookup, [file, native]}.